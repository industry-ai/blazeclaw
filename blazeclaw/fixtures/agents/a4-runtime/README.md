# A4 ACP and Embedded Runtime Fixture

This fixture root is reserved for Wave A4 runtime policy and bridge validations
used by `AcpSpawnService::ValidateFixtureScenarios(...)` and
`PiEmbeddedService::ValidateFixtureScenarios(...)`.
